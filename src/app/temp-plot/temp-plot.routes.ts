import { Routes } from '@angular/router';

import { TempPlotComponent } from './temp-plot.component';

export const TempPlotRoutes: Routes = [
  { path: 'temperature', component: TempPlotComponent }
];
