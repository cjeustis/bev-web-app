import { Routes } from '@angular/router';

import { CreateRecipeComponent } from './recipe-create.component';

export const CreateRecipeRoute: Routes = [
  { path: 'createRecipe', component: CreateRecipeComponent }
];
